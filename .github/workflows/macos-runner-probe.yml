name: Temporary macOS release runner probe

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/macos-runner-probe.yml"

permissions:
  contents: read

jobs:
  probe:
    runs-on: macos-15
    steps:
      - run: uname -m
